const $landing_cards = $('.landing-examples');
const $materialize_modal = $('.modal');
const $error_msg = $('.error-msg');

// Animate CSS
$landing_cards.addClass('animated fadeInLeft');
$error_msg.addClass('animated bounce');

// Materialize Modal
$materialize_modal.modal();